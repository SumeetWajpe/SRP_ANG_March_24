import { createAction } from '@ngrx/store';

export const incrementCount = createAction('[App-Counter-Component] Increment');
export const decrementCount = createAction('[App-Counter-Component] Decrement');
